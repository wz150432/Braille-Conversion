# Changelog

## v1.1.2 (2026-07-28)

### 新增
- **声调切换按钮回归**：工具栏重新加入「⚨ 无调 / ⚨ 带调」按钮 (v1.1.0 误删)

### 修复
- **卡死问题**：去掉后台线程，改为主线程直接调用 `get_page`（首帧 ~5ms），消除线程竞争导致的卡死
- 简化翻页逻辑，去掉 Home/End 跳转避免大文件卡顿

## v1.1.1 (2026-07-28)

### 修复
- **翻页崩溃**：修了页面边界追踪 bug——翻到第二页不再从头重算，也不会卡死
- **.md 文件不可见**：文件对话框默认显示所有支持格式（.txt .md .markdown .rst）
- **线程安全**：后台转换线程加了错误捕获，异常不会导致闪退

## v1.1.0 (2026-07-28)

### 重大改进
- **流式懒加载**：打开文件立即显示第一页，不再等全文转换完成
- 逐页按需转换 + 缓存，翻页和回看几乎瞬间
- 后台预加载相邻页
- resize 后仅重建当前页

## v1.0.1 (2026-07-27)

### 修复
- **数字裸奔**：连续数字不再以 ASCII 泄露，改为 Braille 数字（`123` → `⠼⠁⠃⠉`）
- **数字指示符重复**：多位数字共用一个 `⠼` 前缀
- **符号映射缺失**：`$` `%` `°` `@` `#` `+` `-` `=` 等常用符号全部映射到 Braille
- **中文标点映射**：`，` `。` `！` `？` `：` `；` 等全角标点映射到对应 Braille 符号
- **Markdown 标记抑制**：`#` `-` `*` 等标记在输出中自动清洗

## v1.0.0 (2026-07-27)

🎉 首个正式发布。

### 功能

- **中文 → 盲文**：基于 pypinyin，支持无调/带调模式，遵循 GF 0019-2018
- **英文 → 盲文 Grade 2**：基于 liblouis，缺失自动降级为字母映射
- **LaTeX → Nemeth Code**：自研解析器，支持分式/上下标/求和/积分/根号/希腊字母
- **Markdown 支持**：自动清洗标题/列表/粗体/行内代码/链接标记
- **GUI 桌面应用**：PySide6 构建，支持文件打开/拖入/翻页/缩放重排
- **Braille 点阵渲染**：标准 2×3 点阵，自适应窗口宽度
- **打包分发**：PyInstaller 单文件 exe

### 变更

- 初始版本
